# posix-getopt

# This module is DEPRECATED! See [savonet/ocaml-posix](https://github.com/savonet/ocaml-posix) for the latest work on this API!

This module provides a simple interface for the POSIX `getopt` and its extensions,` getopt_long` and `getopt_long_only`.


